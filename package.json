{
  "name": "shoot-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "网页俯视角射击游戏（无音乐）。逻辑层与渲染层解耦。",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
